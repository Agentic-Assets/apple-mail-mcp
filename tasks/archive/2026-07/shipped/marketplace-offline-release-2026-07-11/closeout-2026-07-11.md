# Marketplace offline release candidate closeout (2026-07-11)

**Branch:** `chore/marketplace-offline-release`
**Base:** `origin/main` at `e4ea9a8`
**Candidate version:** `3.11.3`
**State:** verified source candidate, awaiting signed tag and marketplace client acceptance evidence

## Delivered

- Replaced network-dependent plugin startup with a hash-locked, bundled wheelhouse for macOS arm64 CPython 3.13.
- Added `requirements.lock`, `wheelhouse/`, a repeatable wheelhouse build gate, and an offline-runtime verification gate for both the Claude plugin archive and the MCPB bundle.
- Included the lockfile and wheelhouse in the MCPB payload, then rebuilt the plugin zip and MCPB artifacts.
- Updated all public plugin version surfaces to `3.11.3`, including Codex and Claude plugin manifests, registry metadata, and the MCPB manifest.
- Updated the Codex validator to install the candidate from the local checkout, avoiding a false pass against the public default branch.
- Corrected the Claude cache smoke documentation and MCPB build summary to use the current release version and 41-tool count.

## Verification

- `PYTEST_ADDOPTS='' .venv/bin/pytest tests`: `1547 passed, 48 subtests passed`.
- `bash tools/gates/dev-check.sh release`: passed, including ruff, format, strict mypy, artifact validation, test-count parity, and offline artifact installs.
- `APPLE_MAIL_REQUIRE_DIST_ARTIFACTS=1 bash tools/gates/validate_manifests.sh`: passed (`version=3.11.3`, `tools=41`).
- `bash tools/gates/verify-offline-runtime.sh apple-mail-plugin.zip`: passed with `PIP_NO_INDEX=1`.
- `bash tools/gates/verify-offline-runtime.sh apple-mail-mcp-v3.11.3.mcpb`: passed with `PIP_NO_INDEX=1`.
- `bash tools/gates/validate-codex-plugin.sh`: passed, including a temporary `CODEX_HOME` installation and MCP 41-tool smoke.
- Archive parity audit: the zip and MCPB have no duplicate or directory members; both exactly contain the lockfile and wheelhouse; `apple-mail.plugin` remains byte-identical to `apple-mail-plugin.zip`.

## Boundaries

- This candidate is intentionally platform-specific: macOS arm64 with CPython 3.13. A missing compatible interpreter fails closed rather than downloading dependencies.
- The source candidate is not marketplace-admitted. Its signed release tag and the separate marketplace provenance binding remain required.
- Actual acceptance by Claude, Cursor, ChatGPT, and Mail.app needs client-side evidence. No test result here substitutes for that evidence.
