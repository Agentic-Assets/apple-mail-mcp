# Cursor marketplace source candidate closeout (2026-07-11)

**Branch:** `chore/cursor-marketplace-release`
**Base:** `chore/marketplace-offline-release` at `a98c270`
**Candidate version:** `3.11.4`
**State:** source candidate verified, awaiting protected immutable release tag

## Delivered

- Added the separate Cursor plugin manifest at `plugin/.cursor-plugin/plugin.json`.
- Added the separate Cursor local MCP adapter at `plugin/mcp.json`, with the same relative, draft-safe launcher contract as the verified Codex adapter.
- Added source validation for Cursor name, version, adapter path, launcher shape, working directory, and forbidden Claude path interpolation.
- Added Cursor to public version synchronization and regression tests, then updated source release documentation and the 3.11.4 changelog entry.
- Rebuilt the plugin zip, Cowork `.plugin`, and Claude Desktop MCPB release artifacts. The Cursor files are included in the full plugin archive and intentionally excluded from the Claude-only MCPB payload.

## Verification

- `PYTEST_ADDOPTS='' .venv/bin/pytest tests/infra/test_validate_manifests.py -q`: passed.
- `PYTEST_ADDOPTS='' .venv/bin/pytest tests`: `1548 passed, 48 subtests passed`.
- `APPLE_MAIL_REQUIRE_DIST_ARTIFACTS=1 bash tools/gates/validate_manifests.sh`: passed, version `3.11.4`, 41 tools.
- `bash tools/gates/dev-check.sh release`: passed, including strict ruff, strict mypy, artifact validation, test-count parity, and offline artifact installation.
- `bash tools/gates/validate-codex-plugin.sh`: passed with a clean temporary `CODEX_HOME`, an offline installed candidate, and a 41-tool MCP smoke.
- `bash tools/gates/verify-offline-runtime.sh apple-mail-plugin.zip` and the v3.11.4 MCPB: passed.

## Boundaries

- Static adapter validation and Codex smoke do not prove Cursor accepts the plugin. Cursor Team Marketplace and local client acceptance remain digest-bound marketplace evidence work.
- Do not create or promote a `v3.11.4` source tag until the source repository enforces immutable release tags. A signed but retaggable tag does not meet marketplace provenance policy.
