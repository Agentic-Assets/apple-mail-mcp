# AGENTIC-1277 compose draft recipient verification

## Objective

Verify a standalone compose draft against the exact persisted Drafts recipient
payload, not only its subject and body. The live smoke must remain draft-only
and delete only the exact persisted smoke artifact.

## Evidence and scope

- The 2026-07-11 Exchange smoke created, verified, and removed exact Drafts
  id `119809` without sending.
- `verify_draft` already supports `expected_to`, but
  `draft-verify-smoke` did not pass that expectation through.
- This lane adds that expectation and focused regression coverage. It does not
  alter Mail's compose implementation, send behavior, or any business draft.

## Verification path

1. Focused CLI tests cover recipient mismatch and successful exact-recipient
   verification.
2. The full release gate validates tests, manifests, artifacts, and counts.
3. Run one draft-only live smoke to `cayman@agenticassets.ai` from the Cayman
   Agentic Assets account and confirm exact-ID cleanup.

## Linked work

- Linear: AGENTIC-1277
- Branch: `fix/agentic-1277-compose-draft-recipient-verification`
