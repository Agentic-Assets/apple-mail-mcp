# Closeout: xhigh code review and fixes on the v3.11.3 branch (2026-07-11)

**Branch:** `fix/agentic-1277-compose-draft-recipient-verification`
**Scope:** full /code-review (xhigh, recall mode) of the branch vs `main`, followed by fixes for every confirmed finding, a simplifier pass, expert validation, and live verification. Findings detail: [`review-synthesis-2026-07-11.md`](review-synthesis-2026-07-11.md).

## What shipped in the review-fix pass

1. **Duplicate-recipient cleanup bug fixed** (`tools/compose/cleanup.py`): the count-equality gate that mismatched a deduplicated expected list against raw actual recipients is gone; exact set equality is proven by mutual containment loops, robust to duplicates on either side.
2. **Unknown Drafts total fails open** (`core/reply_state.py`): a missing `TOTAL` marker now parses as `None` and an ok scan with unknown total counts as truncated, so nonmatches report `null` instead of a definitive `false`.
3. **Uniform `draft_scan` envelope**: `total` and `truncated` now appear on every producer (`build_draft_scan_status` empty return, `get_needs_response`, inbox list/overview skipped and error paths; the duplicated `_SKIPPED_DRAFT_SCAN` constants were replaced by `build_draft_scan_status({})`).
4. **osacompile coverage for the delete script**: the identity-guarded delete AppleScript moved into the discoverable `delete_draft_if_identity_matches_script()` builder; a test proves the compile suite exercises it.
5. **Structured account error**: `delete_draft_if_identity_matches` returns `{"deleted": false, "error": "account_resolution_failed", "detail": ...}` instead of a raw string.
6. **One shared recipient normalizer**: `_split_csv_addresses` (now casefold) backs draft verification, the smoke CLI exact-set gate, and the cleanup identity literal; the subsumed `checks`/warning branches in `_draft_verification_passed` were removed.
7. **Docs corrected for three-state `has_draft`**: canonical pre-draft-verification reference plus 4 synced copies, `plugin/skills/CLAUDE.md`, `email-drafting/SKILL.md`, `docs/CLAUDE-conventions.md`, and the `common-workflows.md` null-fallback comments; envelope fields documented, unknown-total truncation included.
8. **Constants cross-reference** distinguishing `DRAFT_SNAPSHOT_CAP` (50, reply-state) from `DRAFT_LOOKUP` (75, compose lookups).
9. CHANGELOG 3.11.3 amended with the six review-fix bullets; `tools/expected_test_count.txt` 1551 → 1559.

## Verification proof

| Gate | Result |
|------|--------|
| `pytest tests/` | 1559 passed, 48 subtests, 0 failed |
| `ruff check` + `ruff format --check` (plugin/apple_mail_mcp) | clean, 106 files |
| `mypy --strict plugin/apple_mail_mcp/` | no issues, 106 files |
| `bash tools/gates/dev-check.sh release` | all checks passed; manifests OK (version 3.11.3, tools 41); artifacts rebuilt with zip/.plugin byte parity; test-count gate OK (1559) |
| Module line budget | OK, no module over 600 LOC |
| plugin-dev:plugin-validator | 8/8 checks pass, zero issues |
| plugin-dev:skill-reviewer | Pass; both minor recommendations applied same day |
| Code-simplifier pass | code already tight; one docstring accuracy fix |

**Live verification (Mail.app, account ai.openclaw):**
- `apple-mail quick-check --json`: ok, all cases pass, mailbox_count 9 via the fixed envelope path.
- `draft-verify-smoke --cleanup`: created, verified, and identity-guard deleted the exact persisted draft (id 119817), no errors.
- `draft-verify-smoke --cleanup --to "<addr>, <ADDR>"` (duplicate-recipient regression): verified true and deleted (id 119834); the pre-fix count gate would have refused and orphaned the draft.
- `apple-mail inbox --json`: live `draft_scan` carries `total`/`truncated` at envelope and per-account level; `has_draft` false only from a complete scan.

## Review method record

Ten independent finder subagents (five correctness angles, reuse, simplification, efficiency, altitude, conventions), main-thread dedup, two inline empirical confirmations replacing the verifier wave at operator request, three parallel fix agents with disjoint file ownership, simplifier, validator, and skill-reviewer passes. Refuted or deliberately kept items are recorded in the synthesis report.

**Merge status:** branch pushed; no PR opened (not authorized for this repo); merge requires Cayman's explicit phrase.
