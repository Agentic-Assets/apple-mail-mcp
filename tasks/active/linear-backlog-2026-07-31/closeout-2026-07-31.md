# Linear backlog closeout (2026-07-31)

**Branch:** `fix/apple-mail-linear-backlog-20260731`
**Base:** `main` at `2d034873f6a30bc1334f4a871d8303c6a819981d`
**Implementation commit:** `c99a63a4e3887a748313e0913e9e0009cfa2c99e`
**Closeout commit:** `a776e51c2675c25db339e081c05a3b2002c153d0`
**State:** shipped in [PR #83](https://github.com/Agentic-Assets/apple-mail-mcp/pull/83), merged to `origin/main` at `ed9e1ee`. The affected Linear issues carry the implementation and verification evidence; protected live gates remain open.

## Goal

Address the actionable Apple Mail MCP Linear backlog without bypassing
founder-controlled issues or performing unapproved mail mutations.

## Completed

- AGENTIC-995 and AGENTIC-996: bounded export supports raw EML, optional
  size-capped attachment bundles, deterministic newest-first page ordering,
  and numeric message-id reporting.
- AGENTIC-1854: `list_events` supports bounded participant-name or address
  filtering while preserving summary privacy.
- AGENTIC-1192, AGENTIC-1904, and AGENTIC-1933: mailbox resolution handles
  special folders and unique nested folder leaves without broad message scans.
- AGENTIC-1989 and AGENTIC-1998: guarded draft deletion rechecks a complete
  identity tuple before deletion; HTML compose uses accessibility body focus
  and removes the exact partial compose window after a focus failure.

## Verification

- `bash tools/gates/dev-check.sh release` passed: strict Ruff and mypy,
  manifest validation (41 tools), artifact rebuild/parity, MCPB unpack and
  validation, task layout, and the mocked test suite.
- `bash tools/gates/validate-codex-plugin.sh` passed, including the 41-tool
  plugin install and MCP runtime smoke.
- `.venv/bin/apple-mail quick-check --json` passed on iCloud: metadata,
  no-hit search, and inbox probes all stayed within their thresholds.
- `git diff --check`, artifact byte parity, the 1,636-test collection count,
  and the module-line budget all passed.

## Decisions and protected gates

- Mailbox fallback enumerates only mailbox metadata, accepts a unique leaf,
  and refuses ambiguous leaf names rather than selecting an arbitrary folder.
- AGENTIC-1093 and AGENTIC-842 were not changed because they retain
  `HOLD:No-Agents`, founder, and human-signoff controls.
- AGENTIC-1191 remains triage-only until a fresh sanitized reproduction
  establishes an actionable failure mode.
- AGENTIC-781 and the Mail-specific portions of the implemented fixes still
  need a human-operated, draft-safe workflow test. Local and bounded live
  smoke evidence is not a substitute for that proof.
