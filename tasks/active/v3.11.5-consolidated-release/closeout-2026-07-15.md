# v3.11.5 consolidated release closeout (2026-07-15)

**Branch:** `chore/consolidate-active-branches-20260715`
**Base:** `origin/main`
**Integration commits:** `253dd3a`, `a6e436b`, `561d178`, `65a50b1`
**Hardening commit:** `475071f`
**State:** locally verified and ready for a combined draft PR; not merged to `main`

## Goal

Review and integrate the four active remote branches for the offline marketplace release, Cursor adapter, compose-draft recipient verification, and account-scoped Sent mailbox resolution. Preserve only changes that add value, resolve cross-branch defects, and produce one locally verified release candidate.

## What shipped on the integration branch

- Merged all four requested branch tips with explicit merge commits so their provenance remains visible.
- Set the combined release version to `3.11.5` across all six version surfaces and rebuilt the Claude Code, Cowork, and MCPB artifacts.
- Added the hash-locked 67-wheel macOS arm64 CPython 3.13 offline runtime and fail-closed launcher from the offline-release branch.
- Added the Cursor manifest and MCP adapter while retaining the shared `plugin/` runtime.
- Preserved exact persisted-recipient verification for compose smoke drafts, rejected empty recipient sets before Mail mutation, and required identity-guarded cleanup readback confirmation.
- Preserved account-scoped Sent mailbox resolution so fallback enumeration cannot escape the selected Mail account.
- Assigned `apple-mail-mcp` to this repository's direct-source marketplace and reserved `agentic-assets` for `Agentic-Assets/Agentic-Assets-Marketplace`.
- Replaced destructive cache refresh behavior with a fail-closed helper that installs and verifies `apple-mail@apple-mail-mcp` without mutating the shared marketplace.

## Adversarial findings resolved

- Changed deletion readbacks from a sticky all-attempts condition to true bounded retries that confirm on the first exact-ID absence and fail closed on errors.
- Removed all legacy `Agentic-Assets` marketplace cleanup logic after live inventory proved that identity belongs to the shared Corbis/central marketplace surface.
- Corrected active documentation to distinguish the PyPI/server Python 3.10+ contract from the bundled Apple Silicon and Python 3.13 contract.
- Fixed Codex installed-plugin JSON parsing, limited marketplace upgrade to the Apple Mail source, restored refresh-helper executable mode, and corrected a release-blocking Ruff import order.

## Verification

- `bash tools/gates/dev-check.sh release`: passed after the final fixes.
- Test-count gate: 1,570 collected and passed.
- Ruff, Ruff format, and `mypy --strict`: passed for 106 production files.
- Manifest, task-layout, repository-root, module-budget, strict Claude plugin, MCPB unpack, and artifact byte-parity gates: passed.
- Offline bootstrap: fresh runtime creation passed from both `apple-mail-plugin.zip` and `apple-mail-mcp-v3.11.5.mcpb` with network disabled.
- Codex: isolated marketplace install and 41-tool MCP handshake passed; repeated `codex plugin add` was idempotent.
- Apple Mail: read-only `apple-mail quick-check --json` passed against the integrated source.
- AppleScript: focused cleanup and account-scoped Sent scripts compiled in the review lanes.
- Adversarial finder and skeptic: all confirmed P1/P2 findings fixed; no remaining release blocker reported.

GitHub Actions were not used. Cursor manifest structure was checked against the current official repository shape, but a live Cursor-client installation was not claimed.

## Decisions made

- Product source repositories remain separate. No Git submodules are required.
- The central marketplace owns the shared `agentic-assets` name and promotes immutable product payloads after source releases exist.
- This repository keeps `apple-mail-mcp` as a collision-free direct-install and maintenance identity.
- The offline plugin payload intentionally supports macOS arm64 with CPython 3.13; the separately installed PyPI/server package retains Python 3.10+ support.

## Left to the operator

- Review the combined draft PR and provide the literal approval phrase required by policy before merge.
- After merge, create the compliant signed annotated `v3.11.5` source tag.
- Promote, admit, attest, and verify that tag in `Agentic-Assets/Agentic-Assets-Marketplace` before advertising `apple-mail@agentic-assets`.
