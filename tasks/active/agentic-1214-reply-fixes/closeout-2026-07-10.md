# Closeout: agentic-1214-reply-fixes (v3.11.1, 2026-07-10)

**Branch:** `fix/agentic-1214-reply-body-truncation` (pushed; no PR, PR creation
not authorized for this repo; merge needs Cayman's explicit phrase).
**Linear:** AGENTIC-1214 (anchor), with AGENTIC-1192 items 1-2, AGENTIC-781
partial closure, AGENTIC-973 cannot-reproduce evidence.

## What shipped

- **Bug 1 + Bug 3 (truncation, ALL CAPS):** native reply body now typed in
  focus-guarded chunks (`typing_scripts.py`; `TYPING_CHUNK_SIZE=80`,
  `TYPING_INTER_CHUNK_DELAY=0.35`, `TYPING_PER_CHUNK_OVERHEAD_SECONDS=0.65`)
  with modifier clears before/around every chunk, per-chunk Mail + System
  Events focus re-checks, scaled timeout with
  `REPLY_BODY_TYPING_BUDGET_EXCEEDED` refusal, and
  `REPLY_BODY_TYPING_INTERRUPTED` mid-typing abort (window discarded).
- **Detection:** post-save verification compares the FULL body above the quote
  (whitespace-flattened, smart-punctuation folded, sentence-start case
  neutralized, then case-sensitive) via a second temp file; one
  delete-and-retype retry gated on id proof (only deletes the draft Mail
  itself reported); `REPLY_BODY_MISMATCH` structured error; success payload
  gained `body_verified` / `retyped` / `stale_artifact_id`.
- **Bug 2:** `manage_drafts(action="create", in_reply_to=...)` refuses with
  `CREATE_CANNOT_THREAD` before AppleScript; standalone guard names the
  calling tool; Exchange draft-id drift documented.
- **AGENTIC-1192 item 2:** `verify_draft`/`verify_drafts`
  `expected_body_contains` scoped above the quote;
  `body_needle_only_in_quote` + `expected_body_only_in_quote` on quote-only
  hits.
- Skills/docs/CHANGELOG updated, including a Known Limitations section from
  the live findings.

## Process

11-agent workflow (4 Sonnet recon, Opus plan, Fable design review with 15
amendments, 2 Sonnet implementers, Sonnet docs, code-simplifier, Sonnet
gates), then an orchestrator (Fable, main thread) adversarial review that
found 4 blocking defects the workflow missed (timeout underestimation,
O(n^2) verifier fold, retry-delete safety hole, dropped tab handling) plus
the B2 include decision, fixed by 3 parallel Sonnet agents. Reports:
`reports/phase1-*.md`, `plan-2026-07-10.md`, `reports/phase2-design-review-2026-07-10.md`,
`reports/phase6-orchestrator-adversarial-review-2026-07-10.md`,
`reports/phase7-live-verification-2026-07-10.md`.

## Proof

- `bash tools/gates/dev-check.sh release` green: ruff, ruff format, mypy
  --strict, all three artifacts rebuilt at 3.11.1 with byte parity, 1536/1536
  tests, count gate, wrapper surface.
- Live verification on `Cayman - Agentic Assets` (Exchange), draft-mode only,
  all 7 test drafts deleted after: ~1200-char body typed fully with exact
  case (13.0s, the field-report defect class); ~5000-char body fully typed
  under the scaled timeout (37.6s); short-body ALL-CAPS class clean 2/2;
  smart punctuation (curly quotes, em dash, ellipsis, lowercase sentence
  start) verified with no false mismatch; `CREATE_CANNOT_THREAD` and
  `body_needle_only_in_quote` contracts confirmed live.
- Live partials (fail closed, documented as Known Limitations + forward
  queue): accented "Renée" corrupted in typing and correctly REJECTED by the
  verifier; a deliberate mid-typing focus steal produced a corrupted draft
  caught by `REPLY_BODY_MISMATCH` rather than the clean interrupted abort;
  the retype net stayed idle behind its id-proof gate.

## Merge notes

Branch already contains origin/main @ v3.11.0 (merged in during the session;
conflicts resolved in CHANGELOG, tasks pointers, test count). Merging this
branch after any other work only needs the usual version-file care.
Follow-ups: `forward-queue-2026-07-10.md`.
