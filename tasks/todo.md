# Active Pointer: apple-mail-mcp

**Tasks layout:** Agents MUST follow [`tasks/CLAUDE.md`](CLAUDE.md) § Agent requirements (`active/` · `reference/` · `archive/` only; local gates enforce).

**Current branch:** `docs/post-merge-task-ledger` (base: `origin/main` @ `b30d9c4`), PR [#93](https://github.com/Agentic-Assets/apple-mail-mcp/pull/93). It carries the release prep for **v3.11.7**: this ledger, the CHANGELOG corrections, and the reproducible plugin-zip build. The signed tag is cut from `main` once it merges.

**Main state:** PRs **#90**, **#91**, and **#92** merged to `main` on 2026-08-19 in that order (`dc56b3c`, `ab04304`, `b30d9c4`). All three shared base `aa7fe0a`, so each was integrated and gated against the *previous* merge rather than against its own PR base. Their closeouts and forward queues are archived under [`archive/2026-08/shipped/`](archive/2026-08/shipped/). The last release tag before this one is **v3.11.6** (`04f9d60`). `pyproject.toml` is at **3.11.7**, and every note for it goes under the dated `## 3.11.7` section rather than `## Unreleased`, which must be empty at gate time. The `## 3.11.7` header was originally dated 2026-08-17, two days before its last three merges; it now reads 2026-08-19 to match the tag, which is this file's convention for every prior release.

**What landed.** One defect class across three branches: an AppleScript `try` with no `on error` arm, which converts a loud failure into an exit-0 wrong answer. `run_applescript` raises on a nonzero `osascript` exit, so a script with *no* `try` is correct; the bare `try` is the bug. Alongside it: the enforced public-repo identity gate (AGENTIC-2358), the `search_emails` subject-fast-path date-window fix (AGENTIC-2356), bounded-scan fallback conversion (AGENTIC-2355), `manage_trash` `dry_run` / `max_deletes` / date-window bounds (AGENTIC-2359), two file-clobber paths (AGENTIC-2361), timeout validation (AGENTIC-2369), and non-positive action caps (AGENTIC-2374).

**Integration hazard, resolved.** #91 introduced two bare `try` blocks in `compose/drafts_scripts.py` that #92's new lint bans. They were fixed at the call site (`windowFailed` and `bodyReadError` error arms, additive to reporting only — deletion control flow untouched), not by raising the ratchet. The two ratchets also collided on key namespace: #90 widened `test_no_unbounded_whose.py` from `TOOLS_DIR` to `PACKAGE_DIR` while #91 ratcheted in the old namespace; the entries are restated under the current namespace with a comment recording why.

**Gate state after merge:** `bash tools/gates/dev-check.sh release` green — 2174 tests (`tools/expected_test_count.txt`), fatal `ruff check` / `ruff format --check` / `mypy --strict`, identity scan, module line budget, artifact rebuild + byte parity, `mcpb unpack + validate`, `claude plugin validate --strict`, tasks layout, repo root. The bare-`try` ratchet baseline now records **229 sites across 50 modules** (171 with no error arm at all, 58 with a silent arm), down from 236/178. Both ratchets are two-sided: a count coming in *under* baseline fails as stale, so regenerate with `python3 tests/core/test_no_bare_applescript_try.py --write-baseline` and confirm every number moved down.

**Live verification actually performed** (2026-08-19, large Exchange mailbox + a 75-draft account):
- AGENTIC-2356 date floor honored on a 25K-message Inbox: floor `2020-01-01` → hit, boundary date → hit (correct `>=`), floor+1 → 0, floor+5 → 0.
- The `manage_drafts` cleanup script edited during integration ran clean over 75 real drafts: exit 0, no runtime error, **0 drafts misclassified as blank**, so 0 deletions proposed. This is what `osacompile` alone cannot prove — the new AppleScript variables resolve at runtime.
- `empty_trash` safety property proved without executing it: with execution stubbed, the default `dry_run` path emits **0** `delete aMessage` statements; only an explicit `dry_run=False` emits one.
- An unresolvable mailbox returns a loud `Error: Mailbox not found`, not a confident `FOUND: 0`.
- `quick-check` ok, 3/3 cases under threshold.

**Still not verified.** #91's destructive paths (`manage_trash`, `update_email_status`) have **never been executed against real mail** under the new code. The safety property above is static proof, not a live deletion test. The disposable-fixture matrix in [`active/native-reply/`](active/native-reply/) is still the outstanding gate and still needs Cayman.

**Distribution.** The `agentic-assets` marketplace admits only allowlisted payload snapshots from signed source tags, so a tag is necessary but not sufficient: until the payload is promoted in the marketplace repo, the installed plugin stays at **v3.11.6** and a live MCP session carries none of this work. That also means MCP tools are the wrong instrument for verifying a fix in this tree — they execute the installed plugin and return a clean answer from stale code. Use `.venv/bin/apple-mail` instead. Promotion is a separate governed step — see [`active/central-marketplace-source-contract/`](active/central-marketplace-source-contract/).

**Reproducible artifact build.** `zip` stamps every entry with its source file's mtime and walks the tree in readdir order, so one commit built in two checkouts produced two different archives — a `git checkout` writes fresh mtimes, and mtimes are what the archive records. A fresh clone therefore saw the tracked `apple-mail-plugin.zip` as drifted with zero content difference (40 entry timestamps moved, 0 CRCs did), and `source-release-gate.sh`'s zero-drift check refused to stamp a release that changed nothing. The build now stages the payload, normalises every mtime to the 1980-01-01 zip epoch, and hands `zip` an `LC_ALL=C`-sorted entry list. Entry names, CRCs, sizes, and permissions are byte-for-byte what the previous build produced, including `0755` on `start_mcp.sh`. Two tests in `tests/infra/test_validate_manifests.py` pin the epoch stamp, the sort order, and the launcher execute bits, because a build that silently loses permissions breaks every install.

**Next action:** cut and push the signed `v3.11.7` tag from `main` after PR #93 merges (`bash tools/gates/create-release-tag.sh` previews; `--confirm-create` signs), then decide on marketplace promotion. After that the highest-value follow-ups by impact are AGENTIC-2372 (`get_awaiting_reply` lists threads that were already answered), AGENTIC-2373 (thread export prints `Exported: 0` on localized/Exchange accounts), AGENTIC-2376 (mutation tools discard partial-work output on error), and AGENTIC-2377. AGENTIC-2375 is a founder policy decision on whether file-writing tools should refuse paths outside the home directory. AGENTIC-781's human-operated native-reply and attachment-contract checks stay open; AGENTIC-1093 and AGENTIC-842 remain founder-controlled; AGENTIC-1191 needs a fresh sanitized reproduction before implementation.

**Roadmap:** [`reference/roadmap-2026-07-10.md`](reference/roadmap-2026-07-10.md): new tools, new skills, enhancements, hardening backlog, and documented macOS refusals. Next three builds: port `get_email_source` forward, add junk + colored-flag actions to `update_email_status`, then the typed-`AppleScriptError` error-contract pass.

## Open lanes

**Live verification, needs Cayman.** [`active/native-reply/`](active/native-reply/): remaining native-reply and attachment-contract TO-TEST items that cannot be mocked, now also carrying the destructive-path gate above. The attachment ordering and fail-closed verifier fix is recorded in [`native-reply-attachment-closeout-2026-08-10.md`](active/native-reply/native-reply-attachment-closeout-2026-08-10.md); its disposable-fixture acceptance matrix and locale-independent quote follow-ups are in [`native-reply-attachment-forward-queue-2026-08-10.md`](active/native-reply/native-reply-attachment-forward-queue-2026-08-10.md). Using a disposable fixture only, live-exercise standalone plain/HTML, native reply/reply-all, explicit-path forward, and rich draft attachments, then confirm proof scope, any current Drafts locator, filename/count/readability, quote/logo preservation where applicable, and `GUARD_ABORT` under real focus contention. Only RFC-backed reply identity may enter the guarded cleanup test; transaction-only iCloud proof is verify-and-inspect only.

**Planning, awaiting sign-off.** [`active/id-first-search-retirement/`](active/id-first-search-retirement/): v4 fuzzy-selector retirement. Decision brief ready for maintainer sign-off; `mailbox="All"` opt-in, v4 legacy-selector removal, and metadata-index live measurement not started. This also owns the open `allow_filter_scan` product decision for `move_email` / `update_email_status` / `manage_trash`.

**Research complete, implementation not started.** [`active/fast-search-index/`](active/fast-search-index/): index-backed fast path for metadata search, measured against a live 87K-message `Envelope Index` in [`research-fast-metadata-search-2026-08-17.md`](active/fast-search-index/research-fast-metadata-search-2026-08-17.md) (AGENTIC-2345). Metadata queries run 0.2 to 74 ms against the index versus roughly 10 s through Mail's window, `messages.ROWID` is the AppleScript `message id`, and the only safe read pattern is a `clonefile(2)` snapshot because a `mode=ro` connection still mutates the `-shm`. Body text is a dead end through any public API. Phase 0 was AGENTIC-2344, shipped in v3.11.7; its AGENTIC-2356 date-window follow-on is now merged and live-verified. Read section 10 before implementing: it holds the open product decisions.

**Distribution evidence open.** [`active/central-marketplace-source-contract/`](active/central-marketplace-source-contract/) (source/identity/signed-tag contract; shared Marketplace admission remains a separately authorized external gate) and [`active/v3.11.6-cursor-adapter/`](active/v3.11.6-cursor-adapter/) (shipped on `main`; local 41-tool Cursor Agent acceptance passed, Cursor marketplace/UI admission unverified).

**Stale, confirm resume-vs-archive.** [`active/v4-performance-consolidation-2026-05-27/`](active/v4-performance-consolidation-2026-05-27/): module-split work shipped (v3.9.1); perf/FTS lanes have not moved since 2026-05-27. [`active/linear-backlog-2026-07-31/`](active/linear-backlog-2026-07-31/) shipped in PR #83 and is retained pending its live gates. Both are over the 30-day archive threshold.

**Other open branch (no task folder).** `fix/github-issues-mcp-hardening-20260617` holds an unmerged `get_email_source` tool (raw RFC822/MIME by id). The roadmap flags porting it forward as the top next build; the branch itself can be dropped once ported.

**Caveats (carried, not blockers):**
- Native reply needs Mail window focus + Accessibility permission (`native_format=False` avoids it; returns `REPLY_WINDOW_FOCUS_FAILED` when focus cannot be acquired).
- Logo not repainted in the reopened draft editor = native Mail behavior (not our bug); SEND-level confirmation still pending.

**Recently shipped** (detail under [`archive/`](archive/)): the three 2026-08-19 merges above, v3.11.7 archive-export skill + search/unread-count fixes (PR #87), v3.11.0 automatic reply-state annotation, v3.10.1 archive human-sender screen, v3.10.0 Apple Calendar surface, v3.9.4 skill-example accuracy, v3.9.1 module line-budget splits, v3.8.0 native-format reply drafts.
