"""Research, smoke, and patch helper scripts for apple-mail-mcp dev-infra.

These are dev-only probes (read-only Mail research, perf JSON comparison, live
MCP smoke, and the mcporter wrapper patch). They are not MCP tools and are not
gates; the gate scripts remain at ``tools/`` root.
"""
