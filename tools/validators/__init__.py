"""Python validator scripts for apple-mail-mcp dev-infra.

Stand-alone validators invoked by ``dev-check.sh`` and the local hooks. They are
not MCP tools and not gates; the shell gates live under ``tools/gates/``. The
manifest check implementations they share live in ``tools/manifest_checks/``.
"""
